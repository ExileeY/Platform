<%= simple_form_for(@comment, url:yield(:comment_form_url), remote: true) do |f| %>
	<%= f.input :body, input_html: {style: 'resize: none; height:100px;'} %>
	<%= f.button :submit, t("project.comments.submit"), class:"btn btn-primary" %>
<% end %>