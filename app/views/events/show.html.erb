<h1><%= t("event.name") %>: <%= @event.title %>.</h1>
<h3><%= t("event.description") %>: <%= @event.body %></h3>
<section><%= image_tag(@event.image.url, alt:'Avatar', id:'galary_image') if @event.image? %></section>
<%= link_to t("event.back_to_project"), project_path(@project)%>